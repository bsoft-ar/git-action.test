name: demo-lab
on: push
jobs:
    dev-deploy:
     runs-on: ubuntu-latest
     steps:
     - name: echo string
       run: echo "hello world"
     - name: commands
       run: |
        npm -v
        node -v
        
